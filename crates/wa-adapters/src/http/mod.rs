//! `HttpTransport` over `reqwest` (rustls). TODO(adapters): implement
//! `ReqwestTransport` with streaming download, multipart, per-request
//! timeout, and error mapping to `TransportError::{Timeout, Connect, Backend}`.
