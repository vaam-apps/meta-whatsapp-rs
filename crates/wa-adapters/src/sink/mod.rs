//! `EventSink` adapters and event streams. TODO(adapters): channel (mpsc),
//! broadcast, fan-out, filter, fn and tracing sinks; `Stream` adapters over
//! broadcast receivers for SSE/WebSocket fan-out.
