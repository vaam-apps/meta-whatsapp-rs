# Feature coverage

What `meta-whatsapp-rs` covers of Meta's WhatsApp Business Platform, in priority order.
Doc paths are relative to
`https://developers.facebook.com/documentation/business-messaging/whatsapp/`.

Status: **done** = implemented and tested against the docs' request/response
examples (each area went through an implementation pass and an adversarial
review with mutation testing); **partial** = the listed subset only;
**planned** = not yet; **out of scope** = deliberately not covered, with the
reason. Verified against the docs as mirrored on 2026-09-24 (Graph API
v25.0).

| # | Feature | Priority | Module | Doc paths | Status |
| --- | --- | --- | --- | --- | --- |
| 1 | Embedded Signup (v4): launch options, session-info events, code exchange, `debug_token`, id verification against the token's grants and Meta's owner lookup, encrypted token vault (AES-256-GCM, key rotation, phone → WABA index), tenant-bound signup sessions, subscribe app, register number, `resume`; an approval gate between verification and storage; per deployment, Solution Partner mode (the approval required and recorded, system user on the WABA, credit line shared with either of Meta's methods after checking it is not already and was not revoked, under a lease renewed before each post and a pending-share flag an operator can clear once Meta shows no live record and no funding the line's records do not explain, unless the operator acknowledged it (audited in the ledger), a revocation racing a share winning, a sealed credit ledger that outlives the token and rotates with it, revocation from the recorded owner, Meta's record of the recorded allocation or a signed webhook's business, typed `CreditError`s, offboarding that revokes before it deletes) | **most requested** | `meta_whatsapp_client::embedded_signup` | `embedded-signup/*`, `access-tokens`, `solution-providers/{share-and-revoke-credit-lines, manage-system-users}` | **done** for the Tech Provider and Solution Partner flows. Pre-verified number pools, multi-WABA onboarding and token refresh are not implemented (product decisions pending). v2/v3 `EsVersion` values are deprecated (Meta: 2026-10-15) |
| 2 | Webhooks: verify token, `X-Hub-Signature-256` (multi-secret, fail-closed; the axum route refuses a missing or malformed header before reading the body), every documented field, BSUID identities, normalized events, leased dedup, PII-free logs, axum router, SSE | very requested | `meta-whatsapp-webhooks` | `webhooks/*`, `business-scoped-user-ids` | **done**, every example payload on every page that prints a webhook body (88 pages, listed with their status in `tests/conformance/manifest.rs`) a fixture checked by `tests/conformance.rs`; the payments pages' payloads are out of scope (row 32), two examples are malformed as printed (recorded there), `webhooks/reference/pricing` and `webhooks/message_echoes` are listed by Meta but answer "Page Not Found", and `message_echoes` / `consumer_profile` (undocumented payloads) arrive as `Unknown`; a standby echo's Send API body, template and Flow definitions stay JSON |
| 3 | Templates: list/get/create/edit/delete, library, migrate, compare, unpause, TTL, named & positional params, media/carousel/LTO/coupon/call-permission/catalog/MPM/SPM/flow components, typed send-time components | very requested | `meta_whatsapp_client::templates` | `templates/*` | **done**; archive/unarchive has no documented endpoint |
| 4 | Authentication templates (copy code, one-tap, zero-tap), previews, bulk upsert, OTP service (E.164-bound, scoped to the sending number and a required tenant namespace, hashed, CAS-counted attempts, cooldown + 5/hour issue limit) | very requested | `meta_whatsapp_client::authentication` | `templates/authentication-templates/*` | **done** |
| 5 | Catalogs & commerce: commerce settings; catalog / single-product / multi-product / product-carousel messages; catalog, MPM, SPM and product-card-carousel templates; `order` webhooks | requested | `meta_whatsapp_client::commerce`, `messages`, `templates`, `meta-whatsapp-webhooks` | `catalogs/*` | **done**; uploading inventory happens in Meta's Catalog API / Commerce Manager, outside the WhatsApp API |
| 6 | In-App Signup (opt-in deep links, ToS acceptance, promo codes) | requested | `meta_whatsapp_client::signups` | `in-app-signup` | **done** |
| 7 | Messages: text, media, location, contacts, address, reactions, stickers, pin/unpin, interactive (buttons, list, CTA URL, location request, flow, media carousel, product carousel, voice call, call permission request, request contact info), contextual replies, mark read, typing indicators, link previews, group sends | core | `meta_whatsapp_client::messages` | `messages/*`, `typing-indicators` | **done**; payments interactive types via the `Raw` escape hatch |
| 8 | Media: upload (MIME/size checked), URL, streaming download with SHA-256 verification (the token goes only to the Graph endpoint and `lookaside.fbsbx.com`), delete, Resumable Upload (template header handles) | core | `meta_whatsapp_client::media` | `business-phone-numbers/media`, `reference/media/*` | **done** |
| 9 | Business-scoped user IDs & usernames: send by BSUID / parent BSUID, identities in every webhook, BSUID-keyed inbox | core (mandatory 2026) | `meta_whatsapp_core::recipient`, `meta-whatsapp-webhooks`, `meta_whatsapp_rs::inbox` | `business-scoped-user-ids` | **done** |
| 10 | Phone numbers: list/get/create, request & verify code, register/deregister, two-step PIN, settings, display names, conversational components, per-number webhook override, coexistence `smb_app_data` sync | core | `meta_whatsapp_client::phone_numbers` | `business-phone-numbers/*`, `display-names` | **done**; payload-encryption settings not wrapped (no documented example) |
| 11 | Business profile (phone number and profile-node APIs) | core | `meta_whatsapp_client::business_profile` | `business-profiles` | **done** |
| 12 | WABA management: details, phone numbers, subscribed apps & callback override, assigned users, client/owned WABAs, customer bases | core | `meta_whatsapp_client::waba` | `whatsapp-business-accounts`, `webhooks/override` | **done**; WABA creation, activities and `system_users` not wrapped |
| 13 | Marketing Messages API for WhatsApp: send (product policy, activity sharing, bid multiplier), onboarding status, client WABAs by onboarding status (paged and streamed), Cloud API marketing switch, partner Intent API | e-commerce | `meta_whatsapp_client::marketing` | `marketing-messages/*` | **done**; max-price agreement, partner allowlist and reach estimates (beta) not wrapped |
| 14 | WhatsApp Flows: management API, business public key, data-endpoint crypto (aws-lc-rs RSA-OAEP + AES-GCM, feature `flows-endpoint`), endpoint signature check, uploaded-media decryption | CMS | `meta_whatsapp_client::flows` | `flows/*` | **done**; Flows metrics API skipped (deprecated by Meta 2026-04-30) |
| 15 | Analytics: messaging, conversation, pricing, template, template-group, call and group analytics; template insights; click-tracking opt-out | marketing | `meta_whatsapp_client::analytics` | `analytics` | **done** |
| 16 | QR codes & short links | marketing | `meta_whatsapp_client::qr_codes` | `qr-codes` | **done** |
| 17 | Block users (phone or BSUID, partial failures) | CMS | `meta_whatsapp_client::block_users` | `block-users` | **done** |
| 18 | Groups API: create, info, list, update, delete, invite links, participants, join requests, pin, picture | CMS | `meta_whatsapp_client::groups` | `groups/*` | **done** |
| 19 | Calling API signalling: settings, permissions, connect/pre-accept/accept/reject/terminate | later | `meta_whatsapp_client::calling` | `calling/*` | **done** (signalling only; WebRTC media out of scope; `media_update` undocumented) |
| 20 | Direct Send (beta): `category`, TTL, direct-send config on free-form sends | later | `meta_whatsapp_client::messages` | `direct-send/*` | **done** |
| 21 | Coexistence (WhatsApp Business app users): onboarding feature type, contacts/history sync, `history` / `smb_app_state_sync` / `smb_message_echoes` webhooks, echoes and synced history recorded by the inbox (direction per the docs, idempotent by message id, chunks in any order except that a revoke whose chunk comes before its message's leaves a tombstone in its place, malformed items skipped) | CMS | `embedded_signup`, `phone_numbers`, `meta-whatsapp-webhooks`, `meta_whatsapp_rs::inbox` | `embedded-signup/onboarding-business-app-users`, `webhooks/reference/history`, `webhooks/reference/smb_message_echoes` | **done**; synced history opens no local window and is never unread (`ConversationStore::append_synced`, one batch per chunk, device timestamps bounded by the sink's clock), a later media content fills its recorded placeholder unless it was revoked (`fill_media_placeholder`), and a revoke deletes only a message of its direction, leaving a tombstone (history only, never in the summary) when it comes first (`revoke`); the contacts sync (`smb_app_state_sync`) is not stored |
| 22 | Error codes → `ErrorKind` (retry safety, 24h window, opt-out, throttling, registration locks) | core | `meta_whatsapp_core::error` | `support/error-codes` | **done** |
| 23 | Typst documents (invoice, receipt, voucher) → PDF/PNG, deterministic, sandboxed | e-commerce | `meta-whatsapp-typst` | — | **done** |
| 24 | Pricing objects on status webhooks (per-message pricing) | analytics | `meta-whatsapp-webhooks` | `pricing` | **done** |
| 25 | Identity change check (`identity_key_hash`, setting) | security | `meta-whatsapp-webhooks`, `phone_numbers` | `identity-change` | **done** |
| 26 | CMS inbox: webhook events → `ConversationStore` (statuses scoped to their business number, content recorded exactly, U+0000 included, coexistence echoes and history: row 21), 24h-window-checked replies with the merchant's token; runnable examples in `crates/meta-whatsapp-rs/examples/` (`cms_inbox` and `embedded_signup` servers with per-tenant bearer auth, `otp_login`, `send_message`, `invoice_document`) | CMS | `meta_whatsapp_rs::inbox` | — | **done**; the window ignores calls, which reopen it on Meta's side (`OPEN_QUESTIONS.md` #32); a revoke matches its number and direction, not its conversation, and a revoked message keeps its content (both decided by the owner, 2026-09-25) |
| 27 | Adapters: reqwest transport; memory, Postgres, Redis stores with executable conformance suites; channel/broadcast/fan-out/filter/fn/tracing sinks | core | `meta-whatsapp-adapters` | — | **done**; the Postgres conversation store keeps U+0000 in message content (`BYTEA` and `json` columns, so SQL-side search is on bytes and payload fields are not indexable) and, like the Postgres key/value store's keys, refuses it in identifiers; Redis requires `noeviction`; Redis TLS needs an integrator-built connection |
| 28 | Solution partner APIs: credit lines, partner-led business verification, multi-partner solutions, WABA/number migration | partners | `meta_whatsapp_client::credit_lines`, `meta_whatsapp_client::business_verification` | `solution-providers/*` | **partial**: credit lines done (list, share and attach in one call or two, verify, find, revoke, status; used by Embedded Signup's Solution Partner mode). Partner-led business verification done (submit with documents checked locally, list submissions, business verification status; the outcome is the `account_update` `PARTNER_CLIENT_CERTIFICATION_STATUS_UPDATE` webhook, `meta_whatsapp_webhooks::fields::PartnerClientCertificationInfo`). Multi-Partner Solutions and migration planned |
| 29 | Conversation routing / handover (standby, thread control) | later | `meta-whatsapp-webhooks` | `conversation-routing/*`, `webhooks/reference/messaging-handovers`, `webhooks/reference/standby` | **partial**: the webhooks are typed (`messaging_handovers` → `ThreadControlChanged`, `standby` → `StandbyObserved`, `conversation_context` on `MessageReceived`); the Thread control API (`pass`, `take`, `release`) is not wrapped; the inbox does not track thread ownership, drops `StandbyObserved` and does not store `conversation_context` (`OPEN_QUESTIONS.md` #44) |
| 30 | Account model evolution (Messaging Accounts, beta) | later | — | `account-model-evolution/*` | planned |
| 31 | CTWA welcome message sequences | later | — | `ctwa/welcome-message-sequences` | planned |
| 32 | Payments (India UPI, Brazil Pix/Boleto) | — | — | `payments/*` | out of scope (market-specific; revisit on demand) |
| 33 | HTTP service for apps not written in Rust (docs/design/server.md): one deployment per Meta app, tenants, tenant/platform/admin keys (SHA-256 digests, constant-time compare, shown once, expiry, suspend and revoke, no cache), fail-closed configuration, public and internal listeners (header read timeout, connection caps, request deadline), Postgres tables and migrations under an advisory lock (expand-only), the authorization order (key, tenant, scope, ownership before the vault), admin API (tenants, keys, platform keys, attaching an own WABA verified with Meta and subscribed, reading a binding, D4 unbind that deletes the token, vault key rotation) and CLI (first admin key, list and revoke keys, `vault rotate`), numbers and business profile routes, disconnect; sends (text, media by id or https link, location, contacts, reaction, template, interactive button/list/cta_url; strict E.164 recipients, BSUIDs, groups), read receipts and typing indicators, media upload (type and size checked first), download verified against Meta's SHA-256 before the first byte (or aborted mid-stream, the last chunk held back) and delete, media and template ids checked to be the path's number's or WABA's (Meta's `phone_number_id`; the WABA's own template list), per-tenant shares of the media and stream slots, template list (cached 60 s per tenant's WABA), get, create (checked locally, no key of Meta's JSON dropped) and delete, `Idempotency-Key` on sends, uploads and template creation (tenant-scoped, replayed, released when nothing was sent, leased, 24 h), per-tenant token-bucket rate limits by route class; Meta's webhooks in (`POST /webhooks/meta`: signature against every app secret before the body, 3 MiB, 64 deliveries read and 4 recorded at once per replica, 15 s per body, the library's leased dedup shared by every replica, routing to the tenant owning the number or WABA since before the event as an allow-list, operator-only rows for unowned numbers, replays and `unknown`, `unparsed` and unreviewed types, inbox then event outbox, idempotent redelivery, errors and bodies that are not webhooks deduplicated for an hour only (then recorded again as a new event), on Postgres the routing checked again at the insert under a lock on the binding, ids derived from the event under the app secret), the event outbox (a sequence per tenant, committed in order; retention purge on one replica at a time; a deleted tenant's events deleted with it), `GET /v1/events` (scope `events`: `after` cursor, `types`, `phone_number_id`, `limit`, 8 MiB pages, `410 cursor_expired`), event data pinned by snapshots over Meta's examples, webhook metrics; error model on `ErrorKind::as_str` (section 5.2 read by the tests), health, Prometheus metrics, request logs with the key id, audit events, committed OpenAPI 3.1 document | TypeScript/CMS callers | `meta-whatsapp-server` (binary crate), on `meta-whatsapp-server-core` (its framework-free core: domain, authorization, error model, storage ports) | `whatsapp-business-accounts`, `business-profiles`, `reference/whatsapp-business-account/{phone-number-management-api, subscribed-apps-api}`, `messages/send-messages`, `messages/*-messages`, `messages/mark-message-as-read`, `typing-indicators`, `business-phone-numbers/media`, `reference/media/*`, `templates/template-management`, `templates/overview`, `reference/whatsapp-business-account/message-template-api`, `webhooks/{overview, create-webhook-endpoint}` | **partial**: milestones M1a, M1b and M1c (acceptance tests M1.1 to M1.7). Inbox routes, `GET /v1/events/{id}`, SSE, webhooks-out, the service's number events (M2); Embedded Signup, OTP, authentication templates, tenant settings (per-tenant rate limits among them), coexistence sync (M3); the documents route, Docker image and TypeScript client (M4) are planned. Event retention defaults to 7 days pending the owner's decision D10; per-tenant sequences (design D21), deleting a deleted tenant's events (D22) and the hour keyless events are deduplicated for are coordinator's decisions awaiting the owner; an undated event (an error) routed just before its tenant was deleted, created again and bound again reaches the new tenant; event ids change with an app secret rotation for events recorded again after their row was purged; one transaction per delivery (instead of per event) is not done; a number moved to another tenant keeps its inbox history under it (M2's inbox reads must filter by binding epoch, or D10 purges on unbind); the design's TOML configuration file and OpenTelemetry export are not implemented; the template cache and rate limits are per replica; that Meta's `phone_number_id` check accepts media received by webhook (not only uploaded) is undocumented (OPEN_QUESTIONS.md #43); attaching a token is not checked for reach over other tenants' WABAs (`debug_token`), only each id a route takes; template shapes the library cannot carry (payment buttons' `order_details` and `payment_request`, `app_deep_link`, `optimization_spec`, the pre-v21 one-tap button fields) are refused, `422` on the key; a template id is looked for in its WABA's first 5 pages of 100 of its name, through the list's undocumented `name=` filter |
