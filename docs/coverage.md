# Feature coverage

What `meta-whatsapp-rs` covers of Meta's WhatsApp Business Platform, in priority order.
Doc paths are relative to
`https://developers.facebook.com/documentation/business-messaging/whatsapp/`.

This is the per-feature list. [parity.md](parity.md) compares the same
code with Zaileys and Meta's Cloud API, capability by capability, and
[categories.md](categories.md) sums it up by Meta platform category (each
of its rows links back to the rows here); [roadmap.md](roadmap.md) plans
what is missing. A PR that changes a feature updates its row here and in
those tables.

Status: **done** = implemented and tested against the docs' request/response
examples (each area went through an implementation pass and an adversarial
review with mutation testing); **partial** = the listed subset only;
**planned** = not yet; **out of scope** = deliberately not covered, with the
reason. Verified against the docs as mirrored on 2026-09-24 (Graph API
v25.0).

Grading follows the rule stated once in
[parity.md § How to read the table](parity.md#how-to-read-the-table) (so a
row with a documented endpoint or field not wrapped is partial, naming
it); this file's **planned** is parity.md's gap, and its **out of scope**
parity.md's n/a.

| # | Feature | Priority | Module | Doc paths | Status |
| --- | --- | --- | --- | --- | --- |
| <a id="row-1"></a>1 | Embedded Signup (v4): launch options, session-info events, code exchange, `debug_token`, id verification against the token's grants and Meta's owner lookup, encrypted token vault (AES-256-GCM, key rotation, phone → WABA index), tenant-bound signup sessions, subscribe app, register number, `resume`; an approval gate between verification and storage; per deployment, Solution Partner mode (the approval required and recorded, system user on the WABA, credit line shared with either of Meta's methods after checking it is not already and was not revoked, under a lease renewed before each post and a pending-share flag an operator can clear once Meta shows no live record and no funding the line's records do not explain, unless the operator acknowledged it (audited in the ledger), a revocation racing a share winning, a sealed credit ledger that outlives the token and rotates with it, revocation from the recorded owner, Meta's record of the recorded allocation or a signed webhook's business, typed `CreditError`s, offboarding that revokes before it deletes) | **most requested** | `meta_whatsapp_client::embedded_signup` | `embedded-signup/*`, `access-tokens`, `solution-providers/{share-and-revoke-credit-lines, manage-system-users}` | **partial** (was "done" until 2026-09-26, by the grading above; [parity.md](parity.md) rows 6, 121–123): the Tech Provider and Solution Partner flows are done; the pre-verified number endpoints (`embedded-signup/pre-verified-numbers`), hosted Embedded Signup (`embedded-signup/hosted-es`), multi-WABA onboarding and a token-expiry signal are not (decided on 2026-09-26: `OPEN_QUESTIONS.md` #5, #8 and #11, [roadmap](roadmap.md) L11a–L11e). v2/v3 `EsVersion` values are deprecated (Meta: 2026-10-15) |
| <a id="row-2"></a>2 | Webhooks: verify token, `X-Hub-Signature-256` (multi-secret, fail-closed; the axum route refuses a missing or malformed header before reading the body), every documented field, BSUID identities, normalized events, leased dedup, PII-free logs, axum router, SSE | very requested | `meta-whatsapp-webhooks` | `webhooks/*`, `business-scoped-user-ids` | **done**, every example payload on every page that prints a webhook body (88 pages, listed with their status in `tests/conformance/manifest.rs`) a fixture checked by `tests/conformance.rs`; the payments pages' payloads are row 32's (planned: [roadmap](roadmap.md) P1), two examples are malformed as printed (recorded there), `webhooks/reference/pricing` and `webhooks/message_echoes` are listed by Meta but answer "Page Not Found", and `message_echoes` / `consumer_profile` (undocumented payloads) arrive as `Unknown`; a standby echo's Send API body, template and Flow definitions stay JSON |
| <a id="row-3"></a>3 | Templates: list/get/create/edit/delete, library, migrate, compare, unpause, TTL, named & positional params, media/carousel/LTO/coupon/call-permission/catalog/MPM/SPM/flow components, typed send-time components | very requested | `meta_whatsapp_client::templates` | `templates/*` | **partial** (was "done" until 2026-09-26, [parity.md](parity.md) rows 101, 153): archiving and unarchiving in bulk (`templates/template-management` says the API does it and sends to `templates/template-archival` for the endpoints, which the mirrored page does not show) and template groups management (in Meta's changelog, its guide not in Meta's page list) are not wrapped |
| <a id="row-4"></a>4 | Authentication templates (copy code, one-tap, zero-tap), previews, bulk upsert, OTP service (E.164-bound, scoped to the sending number and a required tenant namespace, hashed, CAS-counted attempts, cooldown + 5/hour issue limit) | very requested | `meta_whatsapp_client::authentication` | `templates/authentication-templates/*` | **done** |
| <a id="row-5"></a>5 | Catalogs & commerce: commerce settings; catalog / single-product / multi-product / product-carousel messages; catalog, MPM, SPM and product-card-carousel templates; `order` webhooks | requested | `meta_whatsapp_client::commerce`, `messages`, `templates`, `meta-whatsapp-webhooks` | `catalogs/*` | **done**; uploading inventory happens in Meta's Catalog API / Commerce Manager, outside the WhatsApp API |
| <a id="row-6"></a>6 | In-App Signup (opt-in deep links, ToS acceptance, promo codes) | requested | `meta_whatsapp_client::signups` | `in-app-signup` | **done** |
| <a id="row-7"></a>7 | Messages: text, media, location, contacts, address, reactions, stickers, pin/unpin, interactive (buttons, list, CTA URL, location request, flow, media carousel, product carousel, voice call, call permission request, request contact info), contextual replies, mark read, typing indicators, link previews, group sends | core | `meta_whatsapp_client::messages` | `messages/*`, `typing-indicators` | **done**; payments interactive types via the `Raw` escape hatch |
| <a id="row-8"></a>8 | Media: upload (MIME/size checked), URL, streaming download with SHA-256 verification (the token goes only to the Graph endpoint and `lookaside.fbsbx.com`), delete, Resumable Upload (template header handles) | core | `meta_whatsapp_client::media` | `business-phone-numbers/media`, `reference/media/*` | **done** |
| <a id="row-9"></a>9 | Business-scoped user IDs & usernames: send by BSUID / parent BSUID, identities in every webhook, BSUID-keyed inbox | core (mandatory 2026) | `meta_whatsapp_core::recipient`, `meta-whatsapp-webhooks`, `meta_whatsapp_rs::inbox` | `business-scoped-user-ids` | **partial** (was "done" until 2026-09-26, [parity.md](parity.md) rows 66, 151, 152): sending by BSUID or parent BSUID, the identities in every webhook and the BSUID-keyed inbox are done; the same page documents the contact book (deleting an entry), the parent BSUID accounts API and the business username calls, not wrapped |
| <a id="row-10"></a>10 | Phone numbers: list/get/create, request & verify code, register/deregister, two-step PIN, settings, display names, conversational components, per-number webhook override, coexistence `smb_app_data` sync | core | `meta_whatsapp_client::phone_numbers` | `business-phone-numbers/*`, `display-names` | **partial** (was "done" until 2026-09-26: the reference documents fields and endpoints not wrapped, [parity.md](parity.md) rows 66, 129–133): the business username, search visibility, security notifications and number-change notices (`reference/whatsapp-business-phone-number/whatsapp-business-account-phone-number-api`), the Official Business Account request and status, business compliance information (India), bot details; payload-encryption settings (no documented example) |
| <a id="row-11"></a>11 | Business profile (phone number and profile-node APIs) | core | `meta_whatsapp_client::business_profile` | `business-profiles` | **done** |
| <a id="row-12"></a>12 | WABA management: details, phone numbers, subscribed apps & callback override, assigned users, client/owned WABAs, customer bases | core | `meta_whatsapp_client::waba` | `whatsapp-business-accounts`, `webhooks/override` | **partial** (was "done" until 2026-09-26, [parity.md](parity.md) rows 90, 127, 146): WABA creation, activities, a WABA's solutions list, its campaign schedules, `system_users` and a user's assigned WABAs are documented in the reference and not wrapped |
| <a id="row-13"></a>13 | Marketing Messages API for WhatsApp: send (product policy, activity sharing, bid multiplier), onboarding status, client WABAs by onboarding status (paged and streamed), Cloud API marketing switch, partner Intent API | e-commerce | `meta_whatsapp_client::marketing` | `marketing-messages/*` | **partial** (was "done" until 2026-09-26, [parity.md](parity.md) row 137): the max price feature (beta: its agreement, the partner allow list, duplicating a template at another price, `marketing-messages/pricing/*`) and reach estimates (`GET /{WABA_ID}/reachestimate`, `marketing-messages/pricing`) are not wrapped |
| <a id="row-14"></a>14 | WhatsApp Flows: management API, business public key, data-endpoint crypto (aws-lc-rs RSA-OAEP + AES-GCM, feature `flows-endpoint`), endpoint signature check, uploaded-media decryption | CMS | `meta_whatsapp_client::flows` | `flows/*` | **done**; Flows metrics API skipped (deprecated by Meta 2026-04-30) |
| <a id="row-15"></a>15 | Analytics: messaging, conversation, pricing, template, template-group, call and group analytics; template insights; click-tracking opt-out | marketing | `meta_whatsapp_client::analytics` | `analytics` | **done** |
| <a id="row-16"></a>16 | QR codes & short links | marketing | `meta_whatsapp_client::qr_codes` | `qr-codes` | **done** |
| <a id="row-17"></a>17 | Block users (phone or BSUID, partial failures) | CMS | `meta_whatsapp_client::block_users` | `block-users` | **done** |
| <a id="row-18"></a>18 | Groups API: create, info, list, update, delete, invite links, participants, join requests, pin, picture | CMS | `meta_whatsapp_client::groups` | `groups/*` | **done** |
| <a id="row-19"></a>19 | Calling API signalling: settings, permissions, connect/pre-accept/accept/reject/terminate | later | `meta_whatsapp_client::calling` | `calling/*` | **partial** (was "done" until 2026-09-26; [parity.md](parity.md) row 135): signalling done, but the per-call `recording` and `transcription` options on connect and accept (`calling/call-recording`, `calling/call-transcription`) are not carried; WebRTC media out of scope; `media_update` undocumented |
| <a id="row-20"></a>20 | Direct Send (beta): `category`, TTL, direct-send config on free-form sends | later | `meta_whatsapp_client::messages` | `direct-send/*` | **done** |
| <a id="row-21"></a>21 | Coexistence (WhatsApp Business app users): onboarding feature type, contacts/history sync, `history` / `smb_app_state_sync` / `smb_message_echoes` webhooks, echoes and synced history recorded by the inbox (direction per the docs, idempotent by message id, chunks in any order except that a revoke whose chunk comes before its message's leaves a tombstone in its place, malformed items skipped) | CMS | `embedded_signup`, `phone_numbers`, `meta-whatsapp-webhooks`, `meta_whatsapp_rs::inbox` | `embedded-signup/onboarding-business-app-users`, `webhooks/reference/history`, `webhooks/reference/smb_message_echoes` | **done**; synced history opens no local window and is never unread (`ConversationStore::append_synced`, one batch per chunk, device timestamps bounded by the sink's clock), a later media content fills its recorded placeholder unless it was revoked (`fill_media_placeholder`), and a revoke deletes only a message of its direction, leaving a tombstone (history only, never in the summary) when it comes first (`revoke`); the contacts sync (`smb_app_state_sync`) is not stored |
| <a id="row-22"></a>22 | Error codes → `ErrorKind` (retry safety, 24h window, opt-out, throttling, registration locks) | core | `meta_whatsapp_core::error` | `support/error-codes` | **done** |
| <a id="row-23"></a>23 | Typst documents (invoice, receipt, voucher) → PDF/PNG, deterministic, sandboxed | e-commerce | `meta-whatsapp-typst` | — | **done** |
| <a id="row-24"></a>24 | Pricing objects on status webhooks (per-message pricing) | analytics | `meta-whatsapp-webhooks` | `pricing` | **done** |
| <a id="row-25"></a>25 | Identity change check (`identity_key_hash`, setting) | security | `meta-whatsapp-webhooks`, `phone_numbers` | `identity-change` | **done** |
| <a id="row-26"></a>26 | CMS inbox: webhook events → `ConversationStore` (statuses scoped to their business number, content recorded exactly, U+0000 included, coexistence echoes and history: row 21), 24h-window-checked replies with the merchant's token; runnable examples in `crates/meta-whatsapp-rs/examples/` (`cms_inbox` and `embedded_signup` servers with per-tenant bearer auth, `otp_login`, `send_message`, `invoice_document`) | CMS | `meta_whatsapp_rs::inbox` | — | **done**; the window ignores calls, which reopen it on Meta's side (`OPEN_QUESTIONS.md` #32); a revoke matches its number and direction, not its conversation, and a revoked message keeps its content (both decided by the owner, 2026-09-25) |
| <a id="row-27"></a>27 | Adapters: reqwest transport; memory, Postgres, Redis stores with executable conformance suites; channel/broadcast/fan-out/filter/fn/tracing sinks | core | `meta-whatsapp-adapters` | — | **done**; the Postgres conversation store keeps U+0000 in message content (`BYTEA` and `json` columns, so SQL-side search is on bytes and payload fields are not indexable) and, like the Postgres key/value store's keys, refuses it in identifiers; Redis requires `noeviction`; Redis TLS needs an integrator-built connection |
| <a id="row-28"></a>28 | Solution partner APIs: credit lines, partner-led business verification, multi-partner solutions, WABA/number migration | partners | `meta_whatsapp_client::credit_lines`, `meta_whatsapp_client::business_verification` | `solution-providers/*` | **partial**: credit lines done (list, share and attach in one call or two, verify, find, revoke, status; used by Embedded Signup's Solution Partner mode). Partner-led business verification done (submit with documents checked locally, list submissions, business verification status; the outcome is the `account_update` `PARTNER_CLIENT_CERTIFICATION_STATUS_UPDATE` webhook, `meta_whatsapp_webhooks::fields::PartnerClientCertificationInfo`). Multi-Partner Solutions and migration planned |
| <a id="row-29"></a>29 | Conversation routing / handover (standby, thread control) | later | `meta-whatsapp-webhooks` | `conversation-routing/*`, `webhooks/reference/messaging-handovers`, `webhooks/reference/standby` | **partial**: the webhooks are typed (`messaging_handovers` → `ThreadControlChanged`, `standby` → `StandbyObserved`, `conversation_context` on `MessageReceived`); the Thread control API (`pass`, `take`, `release`) is not wrapped; the inbox does not track thread ownership, drops `StandbyObserved` and does not store `conversation_context` (`OPEN_QUESTIONS.md` #44) |
| <a id="row-30"></a>30 | Account model evolution (Messaging Accounts, beta) | later | — | `account-model-evolution/*` | planned |
| <a id="row-31"></a>31 | CTWA welcome message sequences | later | — | `ctwa/welcome-message-sequences` | planned |
| <a id="row-32"></a>32 | Payments (India UPI, Brazil Pix/Boleto) | — | — | `payments/*` | planned: in scope since 2026-09-26 (parity), last in the [roadmap](roadmap.md) (P1, P2); today the `payment_configuration_update` webhook only, messages through `MessageContent::Raw`, and the conformance manifest's two payments pages marked `OutOfScope` until P1 types them |
| <a id="row-33"></a>33 | HTTP service for apps not written in Rust (docs/design/server.md): one deployment per Meta app, tenants, tenant/platform/admin keys (SHA-256 digests, constant-time compare, shown once, expiry, suspend and revoke, no cache), fail-closed configuration, public and internal listeners (header read timeout, connection caps, request deadline), Postgres tables and migrations under an advisory lock (expand-only), the authorization order (key, tenant, scope, ownership before the vault), admin API (tenants, keys, platform keys, attaching an own WABA verified with Meta and subscribed, reading a binding, D4 unbind that deletes the token, vault key rotation) and CLI (first admin key, list and revoke keys, `vault rotate`), numbers and business profile routes, disconnect; sends (text, media by id or https link, location, contacts, reaction, template, interactive button/list/cta_url; strict E.164 recipients, BSUIDs, groups), read receipts and typing indicators, media upload (type and size checked first), download verified against Meta's SHA-256 before the first byte (or aborted mid-stream, the last chunk held back) and delete, media and template ids checked to be the path's number's or WABA's (Meta's `phone_number_id`; the WABA's own template list), per-tenant shares of the media and stream slots, template list (cached 60 s per tenant's WABA), get, create (checked locally, no key of Meta's JSON dropped) and delete, `Idempotency-Key` on sends, uploads and template creation (tenant-scoped, replayed, released when nothing was sent, leased, 24 h), per-tenant token-bucket rate limits by route class; Meta's webhooks in (`POST /webhooks/meta`: signature against every app secret before the body, 3 MiB, 64 deliveries read and 4 recorded at once per replica, 15 s per body, the library's leased dedup shared by every replica, routing to the tenant owning the number or WABA since before the event as an allow-list, operator-only rows for unowned numbers, replays and `unknown`, `unparsed` and unreviewed types, inbox then event outbox, idempotent redelivery, errors and bodies that are not webhooks deduplicated for an hour only (then recorded again as a new event), on Postgres the routing checked again at the insert under a lock on the binding, ids derived from the event under the app secret), the event outbox (a sequence per tenant, committed in order; retention purge on one replica at a time; a deleted tenant's events deleted with it), `GET /v1/events` (scope `events`: `after` cursor, `types`, `phone_number_id`, `limit`, 8 MiB pages, `410 cursor_expired`), event data pinned by snapshots over Meta's examples, webhook metrics; error model on `ErrorKind::as_str` (section 5.2 read by the tests), health, Prometheus metrics, request logs with the key id, audit events, committed OpenAPI 3.1 document | TypeScript/CMS callers | `meta-whatsapp-server` (binary crate) | `whatsapp-business-accounts`, `business-profiles`, `reference/whatsapp-business-account/{phone-number-management-api, subscribed-apps-api}`, `messages/send-messages`, `messages/*-messages`, `messages/mark-message-as-read`, `typing-indicators`, `business-phone-numbers/media`, `reference/media/*`, `templates/template-management`, `templates/overview`, `reference/whatsapp-business-account/message-template-api`, `webhooks/{overview, create-webhook-endpoint}` | **partial**: milestones M1a, M1b and M1c (acceptance tests M1.1 to M1.7). Inbox routes, `GET /v1/events/{id}`, SSE, webhooks-out, the service's number events (M2a–M2e); Embedded Signup, OTP, authentication templates, tenant settings (per-tenant rate limits among them), coexistence sync (M3a–M3f); the documents route, Docker image and TypeScript client (M4) are planned. Event retention defaults to 7 days (design D10, decided on 2026-09-26: configurable per store); per-tenant sequences (design D21), deleting a deleted tenant's events (D22) and the hour keyless events are deduplicated for (D23) are coordinator's decisions that stand under the owner's delegation, and the owner confirms D21 and D22 before the first release; the routes for the modules parity requires (M5a–M5l) are planned too ([roadmap](roadmap.md)); an undated event (an error) routed just before its tenant was deleted, created again and bound again reaches the new tenant; event ids change with an app secret rotation for events recorded again after their row was purged; one transaction per delivery (instead of per event) is not done; a number moved to another tenant keeps its inbox history under it (M2a's inbox reads filter by binding epoch: D10 hides it, never purges); the design's TOML configuration file and OpenTelemetry export are not implemented; the template cache and rate limits are per replica; that Meta's `phone_number_id` check accepts media received by webhook (not only uploaded) is undocumented (OPEN_QUESTIONS.md #43); attaching a token is not checked for reach over other tenants' WABAs (`debug_token`), only each id a route takes; template shapes the library cannot carry (payment buttons' `order_details` and `payment_request`, `app_deep_link`, `optimization_spec`, the pre-v21 one-tap button fields) are refused, `422` on the key; a template id is looked for in its WABA's first 5 pages of 100 of its name, through the list's undocumented `name=` filter |
