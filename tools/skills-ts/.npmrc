# Refuse a Node outside `engines` (the gate pins it in .nvmrc), run no
# package's install scripts, and keep the output quiet.
engine-strict=true
ignore-scripts=true
fund=false
audit=false
save-exact=true
